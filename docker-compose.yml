services:
  react-app:
    build: .
    ports:
      - "5173:5173" 
    volumes:
      - .:/app
      - /app/node_modules
    container_name: HMS_React_App
    environment:
      - CHOKIDAR_USEPOLLING=true
